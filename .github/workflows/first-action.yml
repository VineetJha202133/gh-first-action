name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        uses: actions/checkout@v3
      - name: Install NodeJS
        uses: actions/setup-node@v3
